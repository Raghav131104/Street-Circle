import { motion } from "framer-motion";import HeroGraphic from "./HeroGraphic";
export default function Hero({radius,setRadius,count}){const radii=[2,5,10];const container={hidden:{opacity:0},visible:{opacity:1,transition:{staggerChildren:.12,delayChildren:.2}}};const item={hidden:{opacity:0,y:24},visible:{opacity:1,y:0,transition:{duration:.65,ease:[.22,1,.36,1]}}};return <section className="hero" id="top"><motion.div variants={container} initial="hidden" animate="visible"><motion.small variants={item}><span className="status-dot"/> Your local sharing network</motion.small><motion.h1 variants={item}>Good things belong<br/>in the <span>neighborhood.</span></motion.h1><motion.p variants={item} className="desc">Pass along useful items, offer a skill, or find a helping hand—from people who live close enough to say hello.</motion.p><motion.div variants={item} className="radius"><p className="control-label">Explore within</p><div className="radius-buttons">{radii.map(r=><button key={r} className={radius===r?"active":""} onClick={()=>setRadius(r)} aria-pressed={radius===r}>{r} km</button>)}</div></motion.div><motion.div variants={item} className="meta"><span><i className="ri-community-line"/><b>{count}</b> nearby</span><span><i className="ri-shield-check-line"/>Neighbors only</span><span><i className="ri-map-pin-time-line"/>Always local</span></motion.div></motion.div><HeroGraphic/></section>}
